Runtime browser diagnostics from `node server.js` are appended here.

The main file is `krl-vfx-browser.log`. It is newline-delimited JSON, one
record per browser log post. The app also keeps the same boot log in the
browser and can download it with the `B` key or the perf log export.
